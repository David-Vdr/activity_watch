## Purpose
The goal of this repository is to build code that will be able to process and clean activity watch data from different operating systems, devices and users. 

## Data
This script expects a file called `aw-buckets-export.json` in the root directory. This file is not included in the repository. Please add your own before running.